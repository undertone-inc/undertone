# Server env vars (safe to commit as an example)
#
# Usage:
#   cp env.server.example .env.server
#
# Do NOT commit the real .env.server.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/undertone
DATABASE_SSL=false
NODE_ENV=development

# Auth/session settings
SESSION_TTL_DAYS=30
RESET_TTL_MINUTES=30

# OpenAI (for face analysis)
#
# IMPORTANT: Keep this on the SERVER only. Never put API keys in the Expo app.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_IMAGE_DETAIL=high
OPENAI_MAX_OUTPUT_TOKENS=650

# Upload constraints
UPLOAD_MAX_MB=8

# Email (password reset)
RESEND_API_KEY=
# Example format: "Undertone <onboarding@resend.dev>" (use a verified sender for production)
EMAIL_FROM=
APP_NAME=Undertone

# Plan usage limits (optional overrides)
#
# Free:        5 scans / mo,   1 discovery / mo
# Pro monthly: 20 scans / mo, 10 discoveries / mo
# Pro yearly:  250 scans / yr, 120 discoveries / yr
UPLOAD_LIMIT_FREE=5
UPLOAD_LIMIT_PRO=20
UPLOAD_LIMIT_PRO_YEAR=250
DISCOVER_LIMIT_FREE=1
DISCOVER_LIMIT_PRO=10
DISCOVER_LIMIT_PRO_YEAR=120

# RevenueCat (server-side subscription verification)
# IMPORTANT: Use the SECRET API key (sk_...) and keep it on the server only.
REVENUECAT_SECRET_KEY=
REVENUECAT_ENTITLEMENT_ID=undertone_pro
# Set these to your RevenueCat product identifiers (or ensure your identifiers contain 'monthly'/'yearly')
REVENUECAT_PRODUCT_ID_MONTHLY=monthly
REVENUECAT_PRODUCT_ID_YEARLY=yearly

